import { Routes } from '@angular/router';
import { FilterComponent } from '@app/feature/filter/filter.component';

export const routes: Routes = [
  {
    path: '',
    component: FilterComponent,
  }
];
